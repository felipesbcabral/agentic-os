# Diagramas

Arquivos `.excalidraw` — abra em https://excalidraw.com (File → Open) ou na extensao
Excalidraw do VS Code para ver/editar.

- `stack-ladder.excalidraw` — a escada PROMPT → CONTEXT → HARNESS → LOOP → GRAPH
  (`core/00-manifesto.md`)
- `loop-gate-stop.excalidraw` — o ciclo find → act → gate → checker → record → decide,
  com hard-stops (`core/05-loop-engineering.md`)
- `graph-shapes.excalidraw` — as 4 formas: chain, diamond, router, controlled cycle
  (`core/07-graph-engineering.md`)
